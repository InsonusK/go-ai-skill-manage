# Use cases: internal/infrastructure/filesystem

- internal/infrastructure/filesystem
    - Store.Apply, Store.Snapshot
        - Contract
            - WHEN_Created_skills_receive_state_and_skipped_copies_remain_untouched_THEN_declared_result
                - description: [Created skills receive state and skipped copies remain untouched](features/filesystem.feature)
                - input: an empty target; I apply a "create" operation for "sample" containing "body"; I apply a "skip" operation for "sample" containing "changed"; значения и таблицы в сценарии
                - output: результат вызова и наблюдаемое состояние
                - expected_result: ``target file "sample/SKILL.md" equals "body"; target file "sample/SKILL.md" equals "body"; state for "sample" is managed with hash "hash"``
            - WHEN_Update_replaces_obsolete_managed_files_THEN_declared_result
                - description: [Update replaces obsolete managed files](features/filesystem.feature)
                - input: an empty target; I apply a "create" operation for "sample" containing "old"; значения и таблицы в сценарии
                - output: результат вызова и наблюдаемое состояние
                - expected_result: ``target file "sample/SKILL.md" equals "new"; target path "sample/obsolete.txt" exists "false"``
            - WHEN_Removal_only_deletes_managed_directories_THEN_declared_result
                - description: [Removal only deletes managed directories](features/filesystem.feature)
                - input: an empty target; I apply a "create" operation for "sample" containing "body"; значения и таблицы в сценарии
                - output: результат вызова и наблюдаемое состояние
                - expected_result: ``target path "sample" exists "false"``
            - WHEN_Unmanaged_directories_cannot_be_removed_THEN_declared_result
                - description: [Unmanaged directories cannot be removed](features/filesystem.feature)
                - input: an empty target; I apply a "remove" operation for "manual" containing ""; значения и таблицы в сценарии
                - output: результат вызова и наблюдаемое состояние
                - expected_result: ``filesystem error contains "unmanaged"; target path "manual" exists "true"``
            - WHEN_Escaping_output_paths_are_rejected_before_writing_THEN_declared_result
                - description: [Escaping output paths are rejected before writing](features/filesystem.feature)
                - input: an empty target; I apply a "create" operation for "../escape" containing "body"; значения и таблицы в сценарии
                - output: результат вызова и наблюдаемое состояние
                - expected_result: ``filesystem error contains "unsafe"``
    - Store.Snapshot
        - Contract
            - WHEN_Existing_files_cannot_be_planned_as_new_skills_THEN_declared_result
                - description: [Existing files cannot be planned as new skills](features/filesystem.feature)
                - input: an empty target; значения и таблицы в сценарии
                - output: результат вызова и наблюдаемое состояние
                - expected_result: ``snapshot marks "sample" as existing unmanaged entry``
            - WHEN_Symlinks_cannot_be_planned_as_new_skills_THEN_declared_result
                - description: [Symlinks cannot be planned as new skills](features/filesystem.feature)
                - input: an empty target; значения и таблицы в сценарии
                - output: результат вызова и наблюдаемое состояние
                - expected_result: ``snapshot marks "sample" as existing unmanaged entry``
