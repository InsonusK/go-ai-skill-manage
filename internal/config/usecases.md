# Use cases: internal/config

- internal/config
    - Parse, Resolve
        - Contract
            - WHEN_Defaults_for_a_local_source_THEN_declared_result
                - description: [Defaults for a local source](features/config.feature)
                - input: configuration; I resolve configuration; значения и таблицы в сценарии
                - output: результат вызова и наблюдаемое состояние
                - expected_result: ``the configuration is``
            - WHEN_Named_targets_and_global_adapters_THEN_declared_result
                - description: [Named targets and global adapters](features/config.feature)
                - input: configuration; I resolve configuration; значения и таблицы в сценарии
                - output: результат вызова и наблюдаемое состояние
                - expected_result: ``the configuration is``
            - WHEN_Unknown_adapters_fail_THEN_declared_result
                - description: [Unknown adapters fail](features/config.feature)
                - input: configuration; I resolve configuration; значения и таблицы в сценарии
                - output: результат вызова и наблюдаемое состояние
                - expected_result: ``the config error contains "unknown adapter"``
            - WHEN_Invalid_settings_fail_THEN_declared_result
                - description: [Invalid settings fail](features/config.feature)
                - input: configuration; I resolve configuration; значения и таблицы в сценарии
                - output: результат вызова и наблюдаемое состояние
                - expected_result: ``the config error contains "dry_run must be a boolean"``
            - WHEN_JSON_configuration_is_accepted_THEN_declared_result
                - description: [JSON configuration is accepted](features/config.feature)
                - input: configuration; I resolve configuration; значения и таблицы в сценарии
                - output: результат вызова и наблюдаемое состояние
                - expected_result: ``the configuration is``
            - WHEN_Configuration_rejects_invalid_public_values_THEN_declared_result
                - description: [Configuration rejects invalid public values](features/config.feature)
                - input: configuration; I resolve configuration; значения и таблицы в сценарии
                - output: результат вызова и наблюдаемое состояние
                - expected_result: ``the config error contains "<error>"``
            - WHEN_Root_and_settings_targets_conflict_THEN_declared_result
                - description: [Root and settings targets conflict](features/config.feature)
                - input: configuration defining both `target` and `settings.target`
                - output: parse error
                - expected_result: error contains `target cannot be defined both`
            - WHEN_Legacy_settings_target_and_source_syntax_are_preserved_THEN_declared_result
                - description: [Legacy settings target and source syntax are preserved](features/config.feature)
                - input: configuration; I resolve configuration; значения и таблицы в сценарии
                - output: результат вызова и наблюдаемое состояние
                - expected_result: ``the configuration is``
            - WHEN_Temporary_directory_resolves_from_the_configuration_directory_THEN_declared_result
                - description: [Temporary directory resolves from the configuration directory](features/config.feature)
                - input: configuration with relative or absolute `settings.temp_dir`; I resolve configuration relative to `/project`
                - output: resolved request
                - expected_result: `TempDir` is resolved relative to `/project` or preserves an absolute path
            - WHEN_Non_string_temporary_directory_fails_THEN_declared_result
                - description: [Configuration rejects invalid public values](features/config.feature)
                - input: configuration with non-string `settings.temp_dir`
                - output: parse error
                - expected_result: error contains `temp_dir must be a string`
