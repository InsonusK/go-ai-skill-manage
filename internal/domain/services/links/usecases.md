# Use cases: internal/domain/services/links

- internal/domain/services/links
    - Extract, Excluded
        - Contract
            - WHEN_Markdown_and_wiki_links_preserve_labels_and_fragments_THEN_declared_result
                - description: [Markdown and wiki links preserve labels and fragments](features/links.feature)
                - input: link document; I extract links from "docs/main.md"; значения и таблицы в сценарии
                - output: результат вызова и наблюдаемое состояние
                - expected_result: ``references are``
            - WHEN_Inline_code_and_example_fences_are_excluded_THEN_declared_result
                - description: [Inline code and example fences are excluded](features/links.feature)
                - input: link document; I extract links from "main.md"; значения и таблицы в сценарии
                - output: результат вызова и наблюдаемое состояние
                - expected_result: ``references are``
            - WHEN_Folder_exclusions_apply_to_documents_THEN_declared_result
                - description: [Folder exclusions apply to documents](features/links.feature)
                - input: link document; I extract links from "examples/main.md"; значения и таблицы в сценарии
                - output: результат вызова и наблюдаемое состояние
                - expected_result: ``references are``
            - WHEN_Inline_code_in_a_link_label_excludes_the_reference_for_Python_compatibility_THEN_declared_result
                - description: [Inline code in a link label excludes the reference for Python compatibility](features/links.feature)
                - input: link document; I extract links from "main.md"; значения и таблицы в сценарии
                - output: результат вызова и наблюдаемое состояние
                - expected_result: ``references are``
    - Resolve
        - Contract
            - WHEN_Raw_paths_follow_repository_conventions_THEN_declared_result
                - description: [Raw paths follow repository conventions](features/links.feature)
                - input: source paths "docs/a.md,guide.md"; I resolve "<raw>" from "docs/main.md"; значения и таблицы в сценарии
                - output: результат вызова и наблюдаемое состояние
                - expected_result: ``the resolved path is "<path>" and error contains "<error>"``
            - WHEN_Missing_path_inside_a_selected_skill_follows_Python_ownership_resolution_THEN_declared_result
                - description: [Missing path inside a selected skill follows Python ownership resolution](features/links.feature)
                - input: source paths "guide/SKILL.md"; I resolve "./templates/example.md" from "guide/SKILL.md"; значения и таблицы в сценарии
                - output: результат вызова и наблюдаемое состояние
                - expected_result: ``the resolved path is "guide/templates/example.md" and error contains ""``
